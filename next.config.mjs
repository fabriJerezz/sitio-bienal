/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "via.placeholder.com",
      "res.cloudinary.com",
      "images.unsplash.com",
      "assets.aceternity.com",
    ],
  },
};

export default nextConfig;
