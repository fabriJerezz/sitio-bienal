/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "via.placeholder.com",
      "res.cloudinary.com",
      "images.unsplash.com",
      "assets.aceternity.com",
      "media2.giphy.com",
      "media1.giphy.com",
    ],
  },
};

export default nextConfig;
